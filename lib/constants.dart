import 'package:flutter/material.dart';

const bigText = TextStyle(
  color: Colors.white,
  fontSize: 80.0,
  fontWeight: FontWeight.w500,
);

const titleText = TextStyle(
  color: Colors.white,
  fontSize: 30.0,
);

const descriptionText = TextStyle(
  color: Colors.white,
  fontSize: 18.0,
);

const appDescription =
    "Catch all your favorite movies and Tv Shows on one platform. Be informed on whats's trending and release dates of upcoming shows and movies";

const descriptionColor = Color(0xFF5E5C5C);

const padding8 = 8.0;
const padding16 = 16.0;
const padding32 = 32.0;
const padding64 = 64.0;
